<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.delta</groupId>
    <artifactId>delta-game</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>delta-game</name>
    <description>三角洲游戏代练平台</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <delta.version>1.0.0</delta.version>
        <spring-boot.version>3.2.0</spring-boot.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <druid.version>1.2.20</druid.version>
        <knife4j.version>4.3.0</knife4j.version>
        <hutool.version>5.8.23</hutool.version>
        <weixin-java.version>4.6.0</weixin-java.version>
        <aliyun-dysmsapi.version>4.4.0</aliyun-dysmsapi.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <fastjson.version>2.0.43</fastjson.version>
        <easyexcel.version>3.3.3</easyexcel.version>
        <jjwt.version>0.12.3</jjwt.version>
    </properties>

    <modules>
        <module>delta-common</module>
        <module>delta-system</module>
        <module>delta-user</module>
        <module>delta-player</module>
        <module>delta-product</module>
        <module>delta-order</module>
        <module>delta-pay</module>
        <module>delta-message</module>
        <module>delta-cs</module>
        <module>delta-admin</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- Druid -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-3-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- Knife4j -->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <!-- Hutool -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- WxJava miniapp -->
            <dependency>
                <groupId>com.github.binarywang</groupId>
                <artifactId>weixin-java-miniapp</artifactId>
                <version>${weixin-java.version}</version>
            </dependency>

            <!-- 微信支付官方SDK -->
            <dependency>
                <groupId>com.github.wechatpay-apiv3</groupId>
                <artifactId>wechatpay-java</artifactId>
                <version>0.2.17</version>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- FastJSON2 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- EasyExcel -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>${easyexcel.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- 项目内部模块 -->
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-common</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-system</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-user</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-player</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-product</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-order</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-pay</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-message</artifactId>
                <version>${delta.version}</version>
            </dependency>
            <dependency>
                <groupId>com.delta</groupId>
                <artifactId>delta-cs</artifactId>
                <version>${delta.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.30</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases><enabled>true</enabled></releases>
        </repository>
    </repositories>

</project>
